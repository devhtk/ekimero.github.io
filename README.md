# Dokodemo Ekimero (V2)

Japanese train melodies browser/player built with Astro.

## Dev

```bash
npm install
npm run dev
```

Audio files live in `./audio/` and are copied into `./public/audio/` automatically on dev/build.

